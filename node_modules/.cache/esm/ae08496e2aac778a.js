_2aa‍.x([["default",()=>_2aa‍.o]]);class Library {
  constructor (books = []) {
    this.books = books;
  } 

  bookCount(){
    return this.books.length;
  }

  addBook(newBook){
    this.books.push(newBook);
  }

  addBooks(newBooks){
    this.books = [...this.books, ...newBooks];
  }

  printInventory(){
    this.books.forEach((book)=> {
      const {title, author} = book;
      console.log(`${title} by ${author}`);
    });
  };

}

_2aa‍.d(Library);
